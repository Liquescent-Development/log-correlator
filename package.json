{
  "name": "log-correlator",
  "private": true,
  "workspaces": [
    "packages/*",
    "packages/adapters/*"
  ],
  "scripts": {
    "build": "npm run build --workspace packages/query-parser && npm run build --workspaces",
    "build:parallel": "npm run build --workspaces",
    "test": "npm run test --workspaces --if-present",
    "lint": "eslint . --ext .ts,.js",
    "typecheck": "tsc --noEmit",
    "clean": "npm run clean --workspaces --if-present",
    "changelog": "node tools/changelog.js --unreleased",
    "changelog:all": "node tools/changelog.js --all",
    "changelog:preview": "node tools/changelog.js --unreleased --dry-run"
  },
  "devDependencies": {
    "@types/node": "^20.10.0",
    "@typescript-eslint/eslint-plugin": "^6.13.0",
    "@typescript-eslint/parser": "^6.13.0",
    "eslint": "^8.54.0",
    "jest": "^29.7.0",
    "ts-jest": "^29.1.1",
    "typescript": "^5.3.0"
  },
  "engines": {
    "node": ">=16.0.0"
  }
}